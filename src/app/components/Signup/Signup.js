import React from 'react';
import { connect } from 'react-redux';

export const Signup = () => {



    return (
        <>

        </>
    )
}

const mapStateToProps = (state) => {

    return state;
}

export const ConnectedSignup = connect(mapStateToProps)(Signup);